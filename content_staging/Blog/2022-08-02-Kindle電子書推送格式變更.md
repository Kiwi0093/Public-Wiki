---
title: Kindle電子書推送格式變更
date: 2022-08-02
tags:
  - Linux
  - Life
  - Old-Blog
slug: 2022/08/kindle+book+format
description: 三日不讀書,面目可憎
image:
---

>  :::info 
> 本文轉載自舊站存檔。
> :::

# 前言

小弟一直都有用`calibre`+`kindle app`看<ruby><font color="lightblue"><del>好心人無償提供</del></font><rp>(</rp><rt><font color="red">白嫖</font></rt><rp>)</rp></ruby>的電子書的習慣，前一陣子Amazon通知一直以來使用的`mobi`格式要被淘汰，改成`epub`...

<!--truncate-->

# 主要變更

一直以來我都用`calibre-web`來<ruby><font color="lightblue"><del>管理</del></font><rp>(</rp><rt><font color="red">展示</font></rt><rp>)</rp></ruby>手上的電子書，查了一下還好新版有對應這個功能

於是我就很爽快的把Image給換了...（反正之前用的那個有附的轉檔程式不僅新的規範用不到了，舊版時期我也沒用過...

## Calibre-web

```json
version: '3'
services:
  calibre-web:
    image: lscr.io/linuxserver/calibre-web:latest
    container_name: calibre-web
    environment:
      - PUID=1000
      - USE_CONFIG_DIR=true
      - OAUTHLIB_RELAX_TOKEN_SCOPE=1
      - DOCKER_MODS=linuxserver/calibre-web:calibre
    volumes:
      - /Books/lib/location:/books
      - /var/lib/docker/volumes/calibre/config:/config
    labels:
      traefik.enable: true
      traefik.http.routers.calibre-web.rule: Host(`$Your_Domain`)
      traefik.http.routers.calibre-web.tls: true
      traefik.http.routers.calibre-web.tls.certresolver: myresolver
    restart: unless-stopped
```

沒錯，我換成會比較常更新來自`linuxserver.io`的Image

## 效果

![](https://raw.githubusercontent.com/Kiwi0093/graph/master/img/calibre-web-epub.PNG)

我是因為已經把`mobi`檔都清空了所以只看到一個格式，不然會有選單可以選

# 附帶效果

其實不用兩個格式是很好的，以前我都要把抓下來的書先用desktop版的`calibre`轉檔然後等於把兩種格式都上傳到Server上幾乎是雙倍佔空間..現在這樣不僅省了功夫，還可以省空間

一兼二顧非常之好

# 結論

其實我不太清楚`mobi`跟`epub`兩種格式到底有什麼差別...不過可以統一成一種格式也是比較方便的....